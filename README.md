# alejandro-website
Sitio web de Alejandro — configs OpenPage (JSON-first). El constructor vive en el fork mauricioassetium-sketch/openpage; este repo guarda el sitio: JSON editable + export HTML autónomo.
